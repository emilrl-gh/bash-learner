name: Publish to Release

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: K0stad1n/bash-learner@master
      - name: Build
        run: make workflows
      - name: Release
        uses: K0stad1n/bash-learner@v0.1-beta
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: bash-learner
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
